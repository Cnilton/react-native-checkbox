import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    flex: 1,
    backgroundColor: '#398',
  },
  box: {
    borderRadius: 3,
    width: 20,
    height: 20,
    borderWidth: 1,
    borderColor: '#ccc',
  },
  label: {
    color: '#000',
  },
});

export default styles;
